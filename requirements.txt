# Beacon development requirements — pinned to known-good versions.
#
# Install with:
#   uv pip install -r requirements.txt
#   pip install -r requirements.txt
#
# The ML stack (torch / transformers / tokenizers / sentence-transformers /
# safetensors) must be kept in sync; mismatched installs cause ImportErrors
# that are hard to diagnose.  Pin those tightly; everything else uses >=.
#
# To regenerate this file after an intentional upgrade:
#   pip install --upgrade <packages>   # or: uv pip install --upgrade <packages>
#   (then update the pinned versions below)

# ── Core beacon package ────────────────────────────────────────────────────────
-e .

# ── ML stack (keep these four in lock-step) ────────────────────────────────────
torch==2.11.0
tokenizers==0.22.2
transformers==5.5.3
sentence-transformers==5.4.0
safetensors==0.7.0
huggingface-hub==1.10.1

# ── Other direct dependencies (flexible) ──────────────────────────────────────
blake3>=1.0
GitPython>=3.1
numpy>=2.0
rich>=13.0
scikit-learn>=1.3

# ── Tree-sitter core + grammars ────────────────────────────────────────────────
tree-sitter>=0.25
tree-sitter-python>=0.25
tree-sitter-javascript
tree-sitter-typescript
tree-sitter-go
tree-sitter-rust
tree-sitter-java
tree-sitter-c
tree-sitter-cpp
tree-sitter-bash
tree-sitter-lua
tree-sitter-swift
tree-sitter-markdown

# ── Test tooling ────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
